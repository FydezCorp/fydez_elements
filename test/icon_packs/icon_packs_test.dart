import 'package:flutter_test/flutter_test.dart';
import 'package:fydez_elements/fydez_elements.dart';

// void main() {
//   test(
//     "Check that each icon pack has every icon inside [FyIcons] enum.",
//     () {
//       const usecases = FyIcons.values;
//       final iconPacksList = iconPacks;
//       for (IconPack iconPack in iconPacksList) {
//         expect(usecases.every((e) => (iconPack.pack[e] != null)), isTrue);
//       }
//     },
//   );
// }
