import 'package:flutter/material.dart';

abstract class IconList {
  IconData get home;
  IconData get login;
  IconData get logout;
  IconData get eye;
  IconData get user;
  IconData get heart;
  IconData get store;
}
