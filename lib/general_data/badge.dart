import 'package:flutter/material.dart';

class BadgeElement {
  final String text;
  final Color color;
  final Color textColor;

  BadgeElement({
    required this.text,
    required this.color,
    required this.textColor,
  });
}
