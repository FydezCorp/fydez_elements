import 'package:flutter/material.dart';

class BaseBadge {
  final String text;
  final Color color;
  final Color textColor;

  BaseBadge({
    required this.text,
    required this.color,
    required this.textColor,
  });
}
